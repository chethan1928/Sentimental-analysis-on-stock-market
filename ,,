6 +    passage = assessment_data["passage"]                          
    579 +    if isinstance(num_questions, str) and num_questions.isdigit():
    580 +        num_questions = int(num_questions)
    581 +
    582 +    # Determine how many to add
    583 +    add_count = num_questions or 1
    584 +    if isinstance(add_count, str) and add_count.isdigit():        
    585 +        add_count = int(add_count)
